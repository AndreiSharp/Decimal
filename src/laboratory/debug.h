#define debug_int_arr(a, n)         \
  {                                 \
    for (int q = 0; q < (n); ++q) { \
      printf("%d ", (a)[q]);        \
    }                               \
    puts("");                       \
  }
